apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: whispertrans
  labels:
    app: redis
spec:
  clusterIP: None   # headless - allows StatefulSet DNS
  selector:
    app: redis
  ports:
  - name: redis
    port: 6379
    targetPort: 6379
